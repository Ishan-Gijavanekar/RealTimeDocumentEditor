{
  "name": "realtime-document-editor",
  "version": "1.0.0",
  "private": true,
  "description": "MERN TypeScript real-time collaborative document editor with REST APIs, Socket.IO collaboration, comments, versions, and sharing workflows.",
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "npm --prefix backend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev -- --host 127.0.0.1",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "lint": "npm --prefix frontend run lint"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}